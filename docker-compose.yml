version: '3.4'

services:
  # frontend:
  #   image: sosalosa/moneyroundup_frontend
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - 80:80

  backend:
    image: sosalosa/moneyroundup_backend
    # build:
    #   context: ./src
    #   dockerfile: ./Dockerfile
    ports:
      - 80:80
    restart: on-failure
